# fly.toml app configuration file generated for port-flow on 2025-08-28T13:48:16+10:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'port-flow'
primary_region = 'syd'

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  # Pre-built Astro output is served by Nginx; 256 MB keeps one warm
  # Machine without the cost of the previous 1 GB allocation.
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
